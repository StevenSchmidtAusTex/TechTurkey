<div class="text-center textField mainBack">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="like">👍</span>
  </p>
  <p>
    <button class="btn btn-primary">Like this post!</button>
  </p>
</div>
